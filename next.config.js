/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['projectpokemon.org', 'raw.githubusercontent.com'],
  },
};

module.exports = nextConfig;
